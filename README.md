# Camera Hub

A camera ingestion and snapshot management system.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Usage

```bash
python src/main.py
```
# camera-hub
